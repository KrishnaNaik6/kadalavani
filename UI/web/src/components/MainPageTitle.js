const MainPageTitle = ({ title }) => {
  return (
    <div
      style={{ fontSize: 22, color: "#FD0000", textAlign: "left", padding: 10 }}
    >
      {title}
    </div>
  );
};
export default MainPageTitle;
